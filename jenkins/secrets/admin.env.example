# Copy to admin.env (gitignored) on the server / laptop Jenkins host.
# Used by sync-jobs.sh and init.groovy.d/reset-admin.groovy (via compose env_file).

JENKINS_ADMIN_USER=admin
JENKINS_ADMIN_PASS=change-me-strong-password
