# RETIRED — merged into selamnew-collab.conf (single :80 front door).
#
# Use:
#   sudo cp nginx/selamnew-collab.conf /etc/nginx/sites-available/selamnew-collab
#   sudo ln -sfn /etc/nginx/sites-available/selamnew-collab /etc/nginx/sites-enabled/selamnew-collab
#   sudo rm -f /etc/nginx/sites-enabled/selamnew-devops-webhooks
#   sudo nginx -t && sudo systemctl reload nginx
#
# Vault UI remains on :8200 (not proxied). Secrets Room is optional and not required.
